# ข้อควรระวังก่อนเผยแพร่จริง

สำเนานี้นำโค้ดรีเซ็ตรหัสผ่านแอดมินอัตโนมัติออกแล้ว และสร้าง Git history ใหม่ แต่ยังควรตรวจและปรับปรุงส่วนต่อไปนี้ก่อนใช้งานจริง:

1. สร้าง `SECRET_KEY` ใหม่และห้าม Commit `.env`
2. ใช้ `DEBUG=False` และระบุ `ALLOWED_HOSTS` ให้ตรงโดเมน
3. ใช้ PostgreSQL สำหรับระบบจริง ไม่ใช้ SQLite ร่วมกันหลาย instance
4. ตรวจสิทธิ์การเปิดและดาวน์โหลด PDF ที่ยังไม่อนุมัติ
5. เปลี่ยนการอนุมัติ/ลบ/รายการโปรดให้แก้ข้อมูลผ่าน POST เท่านั้น
6. ตรวจ MIME type ของไฟล์ PDF ไม่ใช่แค่นามสกุล
7. จำกัดความยาวและกรองข้อมูลความคิดเห็น
8. ตรวจการทำงาน Cloudinary ใน `serve_pdf_preview()` เพราะโค้ดต้นฉบับมี logic ซับซ้อนและไม่ควรแสดงรายละเอียด environment ใน error response
9. เปลี่ยนรหัสหรือ API key เดิมทั้งหมด หากเคยอยู่ใน Git history เก่า

Repository นี้ไม่มี `.env`, `db.sqlite3`, PDF จริง หรือ `.git` เดิม
